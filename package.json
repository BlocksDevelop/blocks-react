{
  "name": "blocks-react",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "packageManager": "yarn@4.5.1",
  "workspaces": [
    "packages/**/*",
    "services/**/*"
  ],
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
